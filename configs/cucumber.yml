default:
  # parallel: 2
  failFast: true
  forceExit: true
  language: en
  retry: 0
  strict: true
  verbose: true

  name:
    - Adding before hooks to a method

  paths:
    - tests/features/**/*.feature

  import:
    - tests/support/**/*.js
    - tests/steps/**/*.js

  format:
    - "@cucumber/pretty-formatter"

  formatOptions:
    colorsEnabled: true
