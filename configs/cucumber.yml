default:
  # parallel: 2
  verbose: true
  paths:
    - tests/features/**/*.feature
  import:
    - tests/steps/**/*.js
  format:
    - "@cucumber/pretty-formatter"
  formatOptions:
    colorsEnabled: true
